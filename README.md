# Landing Page & Admin Panel Project  
*(HTML, CSS, JavaScript – No Backend, No Database)*

## 📌 Project Overview
This project is a **frontend-based full-stack simulation** consisting of a **Landing Page** and an **Admin Panel**.  
All data is managed using **Browser LocalStorage**, which simulates backend functionality without using any database or server.

The project allows the admin to manage **projects, clients, contact form responses, and newsletter subscriptions**, which are dynamically displayed on the landing page.

---

## 🎯 Objective
- Create a landing page based on a given reference design
- Create an admin panel to manage dynamic content
- Implement CRUD-like operations using **LocalStorage**
- Implement **image cropping** as a bonus feature
- Maintain clean, organized, and reusable code

---

## 🛠️ Technologies Used
- **HTML5** – Structure
- **CSS3** – Styling & responsive layout
- **JavaScript (Vanilla JS)** – Logic & interactivity
- **LocalStorage** – Data persistence
- **Canvas API** – Image cropping (450 × 350)

---

## 📁 Project Folder Structure

